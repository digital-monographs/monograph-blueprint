{
  "name": "monograph-blueprint",
  "version": "1.0.0",
  "description": "## Description",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "myst start",
    "build": "BASE_URL=/monograph-blueprint myst build --html && cp -r ./assets _build/html/assets",
    "init-gh-pages": "myst init --gh-pages"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mystmd": "^1.6.7"
  }
}
